[project]
name = "plot-docs"
version = "0.1.0"
description = "This is the subproject for the Plot doc site."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.19",
]
